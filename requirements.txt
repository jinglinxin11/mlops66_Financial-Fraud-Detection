# Core dependencies - directly imported in the codebase
# Note: PyTorch (torch, torchvision, torchaudio) must be installed separately
# See installation instructions below

pytorch-tabnet>=4.1.0
pandas>=2.0.0
numpy>=1.20.0
scikit-learn>=1.3.0



# Cloud storage (for GCP bucket access)
google-cloud-storage>=2.14.0

# ============================================================
# PyTorch Installation (choose one based on your setup):
# ============================================================
# Option 1: GPU with CUDA 11.8 (recommended for most GPUs)
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# Option 2: GPU with CUDA 12.1 (for newer GPUs)
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# Option 3: CPU-only (no GPU)
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# Option 4: macOS with Metal (Apple Silicon)
#   pip install torch torchvision torchaudio
#
# After installing PyTorch, install other dependencies:
#   pip install -r requirements.txt
# ============================================================
# Why PyTorch is seprate:
# - CUDA versions require custom index URLs (--index-url flag)
# - Conditional syntax in requirements.txt doesn't support index URLs
# - This approach gives users control over GPU/CPU selection
# ============================================================
